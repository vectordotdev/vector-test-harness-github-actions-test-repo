name: CI

on:
  pull_request_review_comment:
    types: [created]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Print debug data
      run: |
        env
        docker info
      
